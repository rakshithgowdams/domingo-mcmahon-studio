/**
 * Spec-aligned re-export. The implementation lives at
 * `src/components/layout/SmoothScrollProvider.tsx`; this file exists so
 * the documented architecture path (`src/providers/...`) also resolves.
 * Both imports point to the same component instance.
 */
export {
  SmoothScrollProvider,
  default,
} from "@/components/layout/SmoothScrollProvider";
